md-toolbar.animate-show.md-whiteframe-z1(ng-show='!showSearch')
  .md-toolbar-tools
    md-button.md-icon-button(ng-click="toggleSidenav('left')" hide-gt-md='' aria-label='Menu')
      i.material-icons menu
    h3
      | Ecomova
    span(flex='')
    md-button.md-icon-button(aria-label='Search' ng-click='toggleSearch()' ng-show="nav.isLoggedIn()")
      i.material-icons search
    md-button.md-icon-button(aria-label='Help')
      i.material-icons help
    md-button.md-icon-button(aria-label='Login' ng-hide="nav.isLoggedIn()")
      a(ui-sref='login')
        i.material-icons account_circle  
    md-button.md-icon-button(aria-label='Logout' ng-show="nav.isLoggedIn()")
      a(ui-sref='logout')
        i.material-icons exit_to_app
md-toolbar.animate-show.md-hue-1.md-whiteframe-z1(ng-show='showSearch')
  .md-toolbar-tools
    md-button.md-icon-button(ng-click='toggleSearch()', aria-label='Menu')
      i.material-icons arrow_back
    h3(role='button', ng-click='toggleSearch()')
    span(flex='100')
      md-autocomplete(md-theme='input', md-input-name='autocompleteField', md-no-cache='ctrl.noCache', md-selected-item='ctrl.selectedItem', md-search-text='ctrl.searchText', md-items='item in ctrl.querySearch(ctrl.searchText)', md-item-text='item.display', placeholder='Search')
        md-item-template
          span(md-highlight-text='ctrl.searchText') {{item.display}}
        div(ng-messages='searchForm.autocompleteField.$error', ng-if='searchForm.autocompleteField.$touched')
          div(ng-message='required')
            | You 
            b must
            |  have a favorite state.
          div(ng-message='minlength') Your entry is not long enough.
          div(ng-message='maxlength') Your entry is too long.
